import React from "react";
import Search from "../Search";
import "./Header.css";

const Header = () => {
  return (
    <div className="header container">
      <Search />
    </div>
  );
};

export default Header;
